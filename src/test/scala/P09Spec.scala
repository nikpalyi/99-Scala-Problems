//import P09.pack
import org.scalatest.Matchers.convertToAnyShouldWrapper

class P09Spec extends UnitSpec {
//
//  "pack" should "Pack consecutive duplicates of list elements into sub lists." in {
//    pack(List('a, 'a, 'a, 'a, 'b, 'c, 'c, 'a, 'a, 'd, 'e, 'e, 'e, 'e)) shouldBe List(List('a, 'a, 'a, 'a), List('b), List('c, 'c), List('a, 'a), List('d), List('e, 'e, 'e, 'e))
//  }
}

